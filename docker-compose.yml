version: "3.9"

services:
  db:
    container_name: db
    image: postgres:13-alpine
    restart: always
    volumes:
      - pgdata:/var/lib/postgresql/data/
    env_file: ./library_back/.env-sample

volumes:
  pgdata:
    name: pgdata
